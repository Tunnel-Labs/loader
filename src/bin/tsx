#!/usr/bin/env bash

win2lin () { f="${1/C://c}"; printf '%s\n' "${f//\\//}"; }

ORIGINAL_DIR=$(pwd)
SCRIPT_DIR=$( cd -- "$( dirname -- "${BASH_SOURCE[0]}" )" &> /dev/null && pwd )
SCRIPT_DIR=$( win2lin $SCRIPT_DIR )
cd $ORIGINAL_DIR

node --loader $SCRIPT_DIR/../esm-loader.mjs -- $@

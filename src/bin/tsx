#!/usr/bin/env bash

ORIGINAL_DIR=$(pwd)
SCRIPT_DIR=$( cd -- "$( dirname -- "${BASH_SOURCE[0]}" )" &> /dev/null && pwd )
SCRIPT_DIR=$( echo "/$SCRIPT_DIR" | sed 's/\\/\//g' | sed 's/://' )
cd $ORIGINAL_DIR

node --loader $SCRIPT_DIR/../esm-loader.mjs -- $@
